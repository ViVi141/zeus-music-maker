# 文件名ASCII转换详细说明

## 📋 概述

本文档详细说明如何从源文件名（可能包含中文、日文、俄文等非ASCII字符）正确转换为Windows系统安全的ASCII文件名。

---

## 🔄 转换流程

文件名转换采用**多阶段处理**的方式，确保最终生成的文件名既安全又可读。

### 流程图

```
源文件名
  ↓
[阶段1: 多语言字符转换]
  ↓
[阶段2: Windows保留字符过滤]
  ↓
[阶段3: ASCII安全验证]
  ↓
[阶段4: 清理和优化]
  ↓
[阶段5: 长度限制]
  ↓
最终ASCII安全文件名
```

---

## 📝 详细转换步骤

### 阶段1: 多语言字符转换 (`chinese_to_pinyin`)

**目的**: 将各种非ASCII字符转换为拉丁字母

**处理逻辑**:

#### 1.1 ASCII字母数字
```rust
if c.is_ascii_alphanumeric() {
    result.push(c);  // 直接保留
}
```
**示例**: `A`, `a`, `0`, `9` → 保持不变

#### 1.2 ASCII标点符号
```rust
match c {
    ' ' | '-' | '_' | '.' | ',' | '!' | '?' | ':' | ';' | '(' | ')' => result.push(c),
    _ => result.push('_'),  // 其他标点替换为下划线
}
```
**示例**: 
- ` ` (空格), `-`, `_`, `.` → 保留
- `@`, `#`, `$`, `%` → 替换为 `_`

#### 1.3 中文字符
```rust
if Self::is_chinese_char(c) {
    let pinyin_result = c.to_pinyin();
    if let Some(pinyin) = pinyin_result {
        result.push_str(&pinyin.plain());  // 转换为拼音
    } else {
        result.push('_');  // 无法转换则用下划线
    }
}
```

**支持的Unicode范围**:
- `0x4E00..=0x9FFF` - CJK统一汉字
- `0x3400..=0x4DBF` - CJK扩展A
- `0x20000..=0x2A6DF` - CJK扩展B
- `0x2A700..=0x2B73F` - CJK扩展C
- `0x2B740..=0x2B81F` - CJK扩展D
- `0x2B820..=0x2CEAF` - CJK扩展E
- `0x2CEB0..=0x2EBEF` - CJK扩展F

**示例**:
- `音乐` → `yinyue`
- `测试` → `ceshi`
- `你好` → `nihao`

#### 1.4 日语假名
```rust
if Self::is_japanese_kana(c) {
    if let Some(romaji) = Self::hiragana_to_romaji(c) {
        result.push_str(romaji);  // 转换为罗马字
    } else {
        result.push('_');
    }
}
```

**支持的字符范围**:
- `0x3040..=0x309F` - 平假名
- `0x30A0..=0x30FF` - 片假名

**示例**:
- `あ` → `a`
- `か` → `ka`
- `が` → `ga`
- `ぱ` → `pa`

#### 1.5 俄语西里尔字母
```rust
if Self::is_russian_cyrillic(c) {
    if let Some(latin) = Self::cyrillic_to_latin(c) {
        result.push_str(latin);  // 转换为拉丁字母
    } else {
        result.push('_');
    }
}
```

**支持的字符范围**: `0x0400..=0x04FF` - 俄语西里尔字母

**示例**:
- `А` → `A`
- `Б` → `B`
- `В` → `V`
- `Ж` → `Zh`
- `Щ` → `Shch`

#### 1.6 西班牙语重音符号
```rust
let normalized = Self::remove_spanish_accents(c);
if normalized != c {
    result.push(normalized);  // 去除重音
}
```

**转换规则**:
- `á`, `à`, `ä`, `â` → `a`
- `é`, `è`, `ë`, `ê` → `e`
- `í`, `ì`, `ï`, `î` → `i`
- `ó`, `ò`, `ö`, `ô` → `o`
- `ú`, `ù`, `ü`, `û` → `u`
- `ñ` → `n`
- `ç` → `c`

**示例**:
- `café` → `cafe`
- `niño` → `nino`
- `José` → `Jose`

#### 1.7 其他字符
```rust
if c.is_whitespace() {
    result.push(' ');  // 保留空白字符
} else {
    result.push('_');  // 其他非ASCII字符替换为下划线
}
```

---

### 阶段2: Windows保留字符过滤 (`to_ascii_safe_pinyin`)

**目的**: 过滤Windows系统不允许的保留字符

**Windows保留字符列表**:
```rust
const WINDOWS_RESERVED_CHARS: &[char] = &['<', '>', ':', '"', '|', '?', '*', '\\', '/'];
```

**处理逻辑**:
```rust
if WINDOWS_RESERVED_CHARS.contains(&c) {
    result.push('_');  // 替换为下划线
} else if c.is_ascii_alphanumeric() || c == '_' || c == '-' || c == ' ' || c == '.' {
    result.push(c);  // 保留安全字符
} else {
    result.push('_');  // 其他字符替换为下划线
}
```

**示例**:
- `file<name>.txt` → `file_name_.txt`
- `path:to/file` → `path_to_file`
- `test?file.txt` → `test_file.txt`

---

### 阶段3: ASCII安全验证

**目的**: 确保所有字符都是ASCII字符

**验证规则**:
- 只允许: ASCII字母数字、下划线、连字符、空格、点号
- 其他所有字符替换为下划线

---

### 阶段4: 清理和优化

**目的**: 提高文件名的可读性

**清理步骤**:

1. **替换连续下划线**
   ```rust
   .replace("__", "_")   // 两个下划线 → 一个
   .replace("___", "_")  // 三个下划线 → 一个
   ```

2. **处理空格和下划线组合**
   ```rust
   .replace(" _", "_")   // 空格+下划线 → 下划线
   .replace("_ ", "_")   // 下划线+空格 → 下划线
   ```

3. **替换连续空格**
   ```rust
   .replace("  ", " ")   // 两个空格 → 一个
   ```

4. **移除首尾下划线和空格**
   ```rust
   .trim_matches('_')    // 移除开头和结尾的下划线
   .trim()               // 移除开头和结尾的空格
   ```

**示例**:
- `__test__file__` → `test_file`
- `  test  file  ` → `test file`
- `test___file` → `test_file`

---

### 阶段5: 长度限制和默认值处理

**目的**: 确保文件名长度合理，处理空文件名

**处理逻辑**:

1. **空文件名检查**
   ```rust
   if cleaned.is_empty() || cleaned.chars().all(|c| c == '_') {
       "track".to_string()  // 返回默认名称
   }
   ```

2. **长度限制**
   ```rust
   if cleaned.len() > 200 {
       let truncated = cleaned.chars().take(197).collect::<String>();
       format!("{}...", truncated)  // 截断并添加省略号
   }
   ```

**示例**:
- 空字符串 → `track`
- 超长文件名 → 截断到197字符 + `...`

---

## 🎯 完整转换示例

### 示例1: 中文文件名
```
输入: "我的音乐文件.mp3"
  ↓ [阶段1: 中文转拼音]
"wodeyinyuewenjian.mp3"
  ↓ [阶段2-3: 保留字符过滤和ASCII验证]
"wodeyinyuewenjian.mp3" (无需修改)
  ↓ [阶段4: 清理优化]
"wodeyinyuewenjian.mp3"
  ↓ [阶段5: 长度检查]
输出: "wodeyinyuewenjian.mp3"
```

### 示例2: 混合语言文件名
```
输入: "音乐&视频_2024年.mp4"
  ↓ [阶段1: 多语言转换]
"yinyue&shipin_2024nian.mp4"
  ↓ [阶段2: 保留字符过滤]
"yinyue_shipin_2024nian.mp4" (& 被替换)
  ↓ [阶段3: ASCII验证]
"yinyue_shipin_2024nian.mp4"
  ↓ [阶段4: 清理优化]
"yinyue_shipin_2024nian.mp4"
  ↓ [阶段5: 长度检查]
输出: "yinyue_shipin_2024nian.mp4"
```

### 示例3: 包含Windows保留字符
```
输入: "test<file>:name?.txt"
  ↓ [阶段1: 字符转换]
"test<file>:name?.txt" (ASCII字符，直接保留)
  ↓ [阶段2: 保留字符过滤]
"test_file__name_.txt" (< > : ? 被替换为 _)
  ↓ [阶段3: ASCII验证]
"test_file__name_.txt"
  ↓ [阶段4: 清理优化]
"test_file_name_.txt" (连续下划线合并)
  ↓ [阶段5: 长度检查]
输出: "test_file_name_.txt"
```

### 示例4: 日语文件名
```
输入: "音楽ファイル.ogg"
  ↓ [阶段1: 多语言转换]
"yinyuefairu.ogg" (音楽 → yinyue, ファイル → fairu)
  ↓ [阶段2-5: 后续处理]
输出: "yinyuefairu.ogg"
```

### 示例5: 俄语文件名
```
输入: "Музыка.mp3"
  ↓ [阶段1: 多语言转换]
"Muzika.mp3" (Музыка → Muzika)
  ↓ [阶段2-5: 后续处理]
输出: "Muzika.mp3"
```

### 示例6: 空文件名或特殊字符
```
输入: "***"
  ↓ [阶段1: 字符转换]
"***" (ASCII字符)
  ↓ [阶段2: 保留字符过滤]
"___" (* 被替换为 _)
  ↓ [阶段3-4: 清理优化]
"" (只包含下划线，被清理为空)
  ↓ [阶段5: 默认值]
输出: "track" (使用默认名称)
```

---

## 🔧 核心函数说明

### `chinese_to_pinyin(input: &str) -> String`

**功能**: 将多语言字符串转换为拉丁字母

**处理顺序**:
1. ASCII字母数字 → 直接保留
2. ASCII标点 → 保留安全符号，其他替换为下划线
3. 中文字符 → 转换为拼音
4. 日语假名 → 转换为罗马字
5. 俄语西里尔字母 → 转换为拉丁字母
6. 西班牙语重音 → 去除重音
7. 其他字符 → 替换为下划线

### `to_ascii_safe_pinyin(input: &str) -> String`

**功能**: 将字符串转换为ASCII安全格式（拼音风格）

**处理流程**:
1. 调用 `chinese_to_pinyin` 进行多语言转换
2. 过滤 Windows 保留字符
3. 确保所有字符都是ASCII安全
4. 清理多余的下划线和空格
5. 处理空文件名（返回默认值）
6. 限制文件名长度（最大200字符）

### `safe_filename_pinyin(input: &str, index: usize) -> String`

**功能**: 生成安全的文件名（拼音风格），带索引备用

**处理流程**:
1. 调用 `to_ascii_safe_pinyin` 获取安全文件名
2. 如果结果为空或只有下划线，使用 `track{index:03}` 格式
3. 否则进一步优化文件名

---

## 📊 字符转换对照表

### 中文常用字符
| 中文 | 拼音 | 说明 |
|------|------|------|
| 音乐 | yinyue | 标准拼音 |
| 视频 | shipin | 标准拼音 |
| 文件 | wenjian | 标准拼音 |
| 测试 | ceshi | 标准拼音 |

### 日语假名
| 假名 | 罗马字 | 说明 |
|------|--------|------|
| あ | a | 平假名 |
| か | ka | 平假名 |
| が | ga | 浊音 |
| ぱ | pa | 半浊音 |

### 俄语字母
| 西里尔 | 拉丁 | 说明 |
|--------|------|------|
| А | A | 大写 |
| Б | B | 大写 |
| В | V | 大写 |
| Ж | Zh | 大写 |
| Щ | Shch | 大写 |

### Windows保留字符
| 字符 | 替换为 | 说明 |
|------|--------|------|
| `<` | `_` | 小于号 |
| `>` | `_` | 大于号 |
| `:` | `_` | 冒号 |
| `"` | `_` | 双引号 |
| `\|` | `_` | 竖线 |
| `?` | `_` | 问号 |
| `*` | `_` | 星号 |
| `\` | `_` | 反斜杠 |
| `/` | `_` | 正斜杠 |

---

## ⚙️ 使用场景

### 场景1: 音频文件转换
```rust
let input_path = Path::new("我的音乐.mp3");
let file_stem = input_path.file_stem().unwrap().to_string_lossy();
let safe_filename = StringUtils::safe_filename_pinyin(&file_stem, 0);
// 结果: "wodeyinyue"
```

### 场景2: 视频文件转换
```rust
let input_path = Path::new("视频文件<2024>.mp4");
let file_stem = input_path.file_stem().unwrap().to_string_lossy();
let safe_filename = StringUtils::to_ascii_safe_pinyin(&file_stem);
// 结果: "shipinwenjian_2024_"
```

### 场景3: 文件冲突处理
```rust
let base_path = PathBuf::from("output/wodeyinyue.ogg");
let unique_path = StringUtils::ensure_unique_path(base_path);
// 如果文件已存在: "output/wodeyinyue_1.ogg"
// 如果文件不存在: "output/wodeyinyue.ogg"
```

---

## 🛡️ 安全保证

### 1. Windows兼容性
- ✅ 过滤所有Windows保留字符
- ✅ 确保路径长度不超过260字符
- ✅ 处理文件名冲突

### 2. 编码安全
- ✅ 所有输出都是ASCII字符
- ✅ 不包含Unicode字符
- ✅ 兼容所有文件系统

### 3. 可读性
- ✅ 保留原始含义（通过拼音）
- ✅ 清理多余符号
- ✅ 优化文件名格式

---

## 📝 注意事项

1. **拼音转换依赖**: 使用 `pinyin` crate 进行中文转拼音，某些生僻字可能无法转换
2. **日语支持**: 目前只支持基本假名，复杂日语可能需要扩展
3. **俄语支持**: 支持基本西里尔字母，特殊字符可能需要扩展
4. **文件名长度**: 限制在200字符以内，为完整路径预留空间
5. **冲突处理**: 自动添加数字后缀，最多尝试1000次

---

## 🔍 调试建议

如果转换结果不符合预期，可以检查：

1. **输入字符类型**: 确认是否为支持的语言
2. **保留字符**: 检查是否包含Windows保留字符
3. **长度限制**: 确认文件名是否超过200字符
4. **空文件名**: 检查是否所有字符都被过滤

---

**最后更新**: 2026年1月  
**文档版本**: 1.0
